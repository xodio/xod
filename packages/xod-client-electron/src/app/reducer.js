const initialState = {
  workspace: null,
};

const settingsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default {
  settings: settingsReducer,
};
