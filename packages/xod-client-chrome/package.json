{
  "name": "xod-client-chrome",
  "version": "0.0.1",
  "description": "XOD project: Client chrome application",
  "scripts": {
    "build": "rm -rf ./dist && mkdir ./dist && cp -R ./node_modules/xod-client-browser/* ./dist && cp -R ./src/* ./dist"
  },
  "engines": {
    "node": "6.0.0"
  },
  "dependencies": {
    "xod-client-browser": "^0.0.1"
  },
  "devDependencies": {
  },
  "author": "Amperka.ru",
  "license": "MIT"
}
