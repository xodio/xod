{
  "name": "xod-client-chrome",
  "version": "0.0.1",
  "description": "XOD project: Client chrome application",
  "scripts": {
    "build": "rm -rf ./dist && mkdir ./dist && cp -R ./src/node_modules/xod-client-browser/* ./dist && cp -R ./src/* ./dist && rm -r ./dist/node_modules"
  },
  "engines": {
    "node": "6.0.0"
  },
  "dependencies": {},
  "devDependencies": {
  },
  "author": "Amperka.ru",
  "license": "MIT"
}
